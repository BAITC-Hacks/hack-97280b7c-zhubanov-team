# Повторная проверка перед демонстрацией

Дата: 23 сентября 2026 года. Код приложения: `a46f23a3ff1d1a49d59abd38bd320800aab59041`, после объединения PR #19–22. Следующий коммит с этими материалами меняет документацию и презентацию, не поведение приложения.

Среда этого компьютера: Windows, Python 3.12.14, Node.js 24.17.0. Для установки командой по-прежнему документирована проверенная ветка Node.js 22.12+.

| Проверка | Результат повторного запуска |
|---|---|
| `python -m pytest -q` | 37 тестов и 47 вложенных проверок прошли |
| `npm --prefix frontend test` | 12 тестов прошли |
| `npm --prefix frontend run lint` | Успешно |
| `npm --prefix frontend run build` | Успешно |
| `python submission/verify.py` | Оба сценария: SHA-256, 29 выпусков, 2 784 строки, 672 часа февраля на турбину, диапазоны и временные границы |
| Реальные CSV, полный API-цикл, UTC | 29 выпусков, 2 784 строки, 28 сравнений, 0 сетевых вызовов погодного клиента |
| Реальные CSV, полный API-цикл, Asia/Almaty | То же покрытие, 0 сетевых вызовов погодного клиента |
| Свежие CSV-выгрузки и `submission/` | Оба файла совпали побайтно с файлами в репозитории |
| Контроллер, реальные данные, полный период | `forecast_published`, затем `unchanged` без повторного расчёта |
| Запуск приложения | API на 8001, Vite на 5174, `/health` вернул `ok` |
| Браузер, реальные данные | Две турбины, 24/48 часов, переход на следующий день, 24 общих часа, сообщение об устаревшем результате исчезает после расчёта |
| Выгрузки из браузера | Фактически сохранены CSV с 96 строками и JSON-паспорт текущего выпуска со сравнением |
| Неактуальный результат и демо | CSV-выгрузка недоступна до успешного актуального расчёта |
| Ошибка и восстановление | Подставной HTTP 503 виден пользователю, следующий реальный запрос успешен |
| Экран 390 px | Горизонтальной прокрутки нет; на настольном экране также показаны реальные графики |
| JavaScript | Необработанных ошибок страницы нет |
| Презентация | 5 слайдов, редактируемые таблица и график, проверена структура PPTX и просмотрены изображения слайдов |

Проверки полного периода использовали настоящие CSV организаторов и 58 ранее скачанных архивных погодных файлов. Вызов `backend.weather.urlopen` был заменён запретом на сетевое обращение и ни разу не вызывался. Сетевой адаптер компьютера не отключали. Проверка не доказывает доступность этих погодных файлов в исторический момент публикации и не измеряет февральскую точность.

Прогон контроллера на реальных CSV проверил начальную публикацию и пропуск неизменных входов. Изменения CSV и погоды, сохранение прошлого результата после ошибок и повторные попытки дополнительно покрыты синтетическими тестами `tests/test_agent.py`. Сырые данные организаторов для этих проверок не менялись.

В pytest остаётся предупреждение зависимости Starlette: устаревает `anyio.abc.BlockingPortal`. Оно не привело к падению тестов.

## GitHub Actions

Последний проверенный запуск на `main`: [35856395987](https://github.com/BAITC-Hacks/hack-97280b7c-zhubanov-team/actions/runs/35856395987). Оба задания не начали выполнять шаги: GitHub сообщил о блокировке аккаунта из-за биллинга. Это инфраструктурный блокер, hosted CI не считается прошедшим. Владелец аккаунта должен устранить блокировку и перезапустить проверки.

Локальные отчёты и скриншоты сохраняются в игнорируемой папке `outputs/demo-preparation/`, свежие выгрузки — в `outputs/submission-a46f23a/`. Они служат протоколом этой репетиции; исходные CSV, кэш и секреты не входят в коммит материалов.
